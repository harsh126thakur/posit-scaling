# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# --- generated results ---
# CSVs are regenerated by `make all` in well under two minutes, so they are
# not tracked. Drop the next line if you want archival results in the repo.
results/*.csv

# --- generated figures ---
# Figures ARE tracked: they are small and show the results in the README.
# (no ignore rule for figures/)

# --- manuscript ---
# The paper is drafted in Overleaf and is deliberately NOT published here.
paper/
*.tex
*.aux
*.synctex.gz

# --- editors / OS ---
.vscode/
.idea/
*.swp
.DS_Store